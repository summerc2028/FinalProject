<% provide(:title, 'Sign In') %>

<div class="container">
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <br />
      <%= render 'application/flash' %>
      <h1 class="page-header">Sign In</h1>
      <%= form_tag sessions_path do %>

        <%= label_tag :username %>
        <%= text_field_tag :username, nil, id: "username-primary", class: "form-control" %>

        <%= label_tag :password %>
        <%= password_field_tag :password, nil, id: "password-primary", class: "form-control" %>

        <%= check_box_tag :remember_me, 1, params[:remember_me] %>
        <%= label_tag :remember_me %>

        <hr />

        <%= submit_tag "Sign In", id: "sign-in-primary", class: "btn btn-primary" %>

      <% end %>
    </div>
  </div>
</div>
